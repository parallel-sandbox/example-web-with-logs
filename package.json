{
  "name": "example-web-with-logs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "npx --yes serve dist"
  },
  "optionalDependencies": {
    "@parallelsandbox/log": "^0.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
